-- Listing record with score >= 10 in a table of a database
-- order by score in descendent, table: second _table, database: argument(hbtn_0c_0)
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
