#include <lists.h>
/**
* check_cycle - Function that checls if a singly linked list has a cycle in it
* @list: list to check
* Return: 0 if there is no cyrcle, 1 if there is a cycle
*/
int check_cycle(listint_t *list)
{
}
